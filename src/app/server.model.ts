export class Server {
    name: string;
    capacity: number;
    id: string;

    constructor(name, capacity, id) {
        this.name = name;
        this.capacity = capacity;
        this.id = id;
    }
}